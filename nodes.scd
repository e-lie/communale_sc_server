(
~makeNodes = {

	s.bind({
		~mainGrp = Group.new;
		// ~radioGrp = Group.after(~mainGrp);





/*~master = Synth.new(
			\master_spat,
			[
				\amp, 0,
				\in, ~bus[\masterSpat],
				\out, 0,
			],
			~masterGrpSpat
		);*/


	});
};
)

